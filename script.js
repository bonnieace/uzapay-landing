document.addEventListener("DOMContentLoaded",()=>{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("on");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(n=>io.observe(n));const form=document.getElementById("waitlistForm");if(form){const success=document.getElementById("success"),submit=document.getElementById("submitBtn");form.addEventListener("submit",e=>{e.preventDefault();submit.disabled=true;submit.textContent="Joining…";setTimeout(()=>{const email=document.getElementById("email").value.trim().toLowerCase(),business=(document.getElementById("businessName")?.value||"").trim();try{const list=JSON.parse(localStorage.getItem("uzapay-waitlist")||"[]").filter(x=>x.email!==email);list.push({email,business,joinedAt:new Date().toISOString()});localStorage.setItem("uzapay-waitlist",JSON.stringify(list));form.style.display="none";success?.classList.add("show")}catch{submit.disabled=false;submit.textContent="Join the waitlist →"}},450)})}});