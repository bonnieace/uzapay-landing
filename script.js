document.addEventListener("DOMContentLoaded",()=>{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("on");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(n=>io.observe(n));const form=document.getElementById("waitlistForm");if(!form)return;const success=document.getElementById("success"),submit=document.getElementById("submitBtn"),email=document.getElementById("email"),honeypot=document.getElementById("website");form.addEventListener("submit",async e=>{e.preventDefault();if(honeypot?.value)return;const value=email.value.trim().toLowerCase();if(!value)return;submit.disabled=true;submit.textContent="Joining…";try{const response=await fetch("/.netlify/functions/waitlist",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:value})});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.message||"Unable to join the waitlist");form.style.display="none";success?.classList.add("show")}catch(err){submit.disabled=false;submit.textContent="Join the waitlist →";alert(err.message||"Something went wrong. Please try again.")}})});